{
  "name": "vocab",
  "version": "0.0.1",
  "description": "A small Vocabulary Trainer WebApp",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run build && firebase deploy",
    "serve": "npm run build && firebase serve",
    "build": "npm run build:clean && npm run build:html && npm run build:sass && webpack --mode=development",
    "build:clean": "rm -rf dist/ && mkdir dist/",
    "build:sass": "sass src/scss/index.scss dist/style.css",
    "build:html": "cp -f src/*.html dist/ && cp -f src/*.png dist/"
  },
  "keywords": [
    "vocabulary",
    "trainer",
    "learn"
  ],
  "author": "Konrad Jahn",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.23.1",
    "webpack-cli": "^3.1.2"
  }
}
